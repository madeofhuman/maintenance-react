import React from 'react';
import logo from '../assets/img/logo.svg';

/**
 * Renders the Logo component on a node in the DOM
 * @returns {object} the Logo component to render
 */
const Logo = () => (
  <img src={logo} alt="logo" />
);

export default Logo;
